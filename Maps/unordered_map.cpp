#include <bits/stdc++.h>
using namespace std;

int main()
{
    unordered_map<int, string> m; // unordered_map is a data structure which stores key value pairs
    // it is not ordered by key, it uses hashing for fast access
    // all the functions of map are available in unordered_map

    // but just the ordered is different and lexicographical order is not maintained

    // Time complexity of unordered_map is O(1) for insertion, deletion and access on average case
}